{
  "name": "@opsa/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API OPSA para os BKs MF0, seguindo o contrato apps/api definido na planificação.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "syntax:check": "find src -name '*.js' -print0 | xargs -0 -n 1 node --check"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcrypt": "^5.1.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
